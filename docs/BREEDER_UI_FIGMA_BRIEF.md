# Brief Figma — Breeder (index)

Brief đầy đủ để gửi Figma nằm tại:

**[MOBILE_BREEDER_DESIGN_CONTEXT.md](./MOBILE_BREEDER_DESIGN_CONTEXT.md)**

File đó gồm: product/roles, IA, visual tokens (`#1E6FE8`), Top Breeders, hồ sơ trại theo template, Farm Health (SHIPPED vs DATA LATER), template picker, profile edit, component list, out of scope, annotation `SHIPPED` / `NEW` / `DATA LATER`.

Bản rút gọn trước đó được thay bằng index này để tránh hai brief lệch nhau.

## Cần PO chốt trước khi Figma khóa

1. Message trên detail hay chỉ qua listing?
2. Deal success/fail: tạm empty hay nguồn data sau?
3. Trust và Farm Health: 1 điểm hay 2 điểm?
4. Template ảnh hưởng cả Top Breeders card hay chỉ trang detail?
