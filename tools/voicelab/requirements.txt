# VoiceLab extra dependencies (on top of torch + omnivoice).
# These are already present in the D:\omnivoice\.venv used for OmniVoice,
# but listed here so the app can be recreated from scratch.
#
# Full setup from zero:
#   uv venv --python 3.12
#   uv pip install torch==2.8.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cpu
#   uv pip install omnivoice
#   uv pip install -r requirements.txt
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
soundfile>=0.12
numpy>=1.26
