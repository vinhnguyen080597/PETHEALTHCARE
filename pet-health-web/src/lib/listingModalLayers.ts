/** Deposit / attach / report overlays on listing detail. */
export const LISTING_ACTION_MODAL_Z_CLASS = "z-50";

/**
 * Warranty policy review must stack above listing action modals
 * (e.g. opened from "View warranty" inside the deposit confirm popup).
 */
export const WARRANTY_POLICY_VIEWER_Z_CLASS = "z-[60]";
